"""Fixture files for document ingestion tests.

Each fixture is a minimal, valid file of its respective format so that tests
can run without any network access or optional system libraries beyond those
declared in ``pyproject.toml``.
"""
